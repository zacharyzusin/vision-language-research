program: slurm/active/run_sweep_training.sh
method: grid
metric:
  name: best_val_acc
  goal: maximize
parameters:
  model.K:
    values: [1, 2, 4, 8, 16, 32, 64]  # 7 values: powers of 2, standard in ML research
project: inat-mop-clip
name: k_sweep_1node
command:
  - bash
  - slurm/active/run_sweep_training.sh

